<Window x:Class="KHSX.Views.BuildGroupInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KHSX.Views"
        mc:Ignorable="d"
        Title="Thông Tin Nhóm BuildGroup" Height="600" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- Style cho Header nhóm (Group Header) -->
        <Style x:Key="GroupHeaderStyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <StackPanel>
                            <Border Background="#E3F2FD" BorderBrush="#90CAF9" BorderThickness="1" CornerRadius="3" Margin="0,5,0,0" Padding="5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="BuildGroup: " FontWeight="Bold" Foreground="#1565C0"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#1565C0"/>
                                    <TextBlock Text=" - Tổng còn lại: " FontWeight="Bold" Foreground="#1565C0" Margin="10,0,0,0"/>
                                    <!-- Để tính tổng cho group, WPF có thể hơi phức tạp nếu chỉ dùng Binding Name. 
                                         Trong code-behind ta sẽ thêm Dictionary để lấy tổng hoặc hiển thị ngay trên DataGrid -->
                                </StackPanel>
                            </Border>
                            <ItemsPresenter />
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Danh sách các BuildGroup và số phút còn lại tương ứng (Open Minutes)"
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap" Foreground="#333"/>

        <DataGrid Grid.Row="1" x:Name="ProductListGrid" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowBackground="White" AlternatingRowBackground="#F9F9F9">
            <DataGrid.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource GroupHeaderStyle}"/>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Sản Phẩm" Binding="{Binding ProductId}" Width="*" />
                <DataGridTextColumn Header="Nhóm BuildGroup" Binding="{Binding GroupId}" Width="*" />
                <DataGridTextColumn Header="Số phút còn lại" Binding="{Binding OpenMinutes}" Width="150" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#D84315"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Đóng" Width="100" Padding="8" Click="btnClose_Click" Background="#f0f0f0"/>
        </StackPanel>
    </Grid>
</Window>
